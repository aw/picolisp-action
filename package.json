{
  "name": "picolisp-action",
  "version": "2.2.0",
  "description": "Run 32-bit or 64-bit PicoLisp code",
  "main": "dist/index.js",
  "scripts": {
    "build": "coffee -c -b --no-header index.coffee && ncc build index.js && rm index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aw/picolisp-action.git"
  },
  "keywords": [
    "picolisp",
    "actions"
  ],
  "author": "AW",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.0",
    "@actions/exec": "^1.0.1"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5",
    "coffeescript": "^2.4.1"
  }
}
